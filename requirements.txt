# Core Django
Django>=3.2,<5.0

# Production WSGI server
gunicorn

# Database adapter for PostgreSQL
psycopg2-binary

# Optional: Django REST Framework (if your project uses APIs)
djangorestframework

# Optional: Environment variable management
python-decouple

# Optional: Static file management
whitenoise

# Optional: For CORS handling in APIs
django-cors-headers
